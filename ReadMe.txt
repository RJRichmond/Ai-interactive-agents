When loading the solution you need to do some basic setup.

Change it to x86.
And in the properties, go to debugging and change the working directory to : $(ProjectDir)..\Build

When running the exe. You can get more information about each agent by clicking on them.
It doesn't automatically update this due to the way I set it up so you will need to click again to see new information.